class Solution {
    public boolean canReach(int[] start, int[] target) {
        int s=start[0]+start[1];
        int t=target[0]+target[1];
        if((s%2==0 && t%2==0) ||(s%2==1 && t%2==1))
            return true;
        else return false;
    }
    
}©leetcode
