class Solution {
    public int[] searchRange(int[] nums, int target) {
        int left=0;
        int right=nums.length-1;
        int []idx=new int[2];

         while(left<=right){
            int mid=(left+right)/2;
              if(nums[mid]==target){
                
          }
        }
        idx[0]=-1;
        idx[1]=-1;
        return idx;
    }
}
